#!/bin/sh /etc/rc.common
START=99
STOP=10

start() {
    mkdir -p /var/lock
    /usr/bin/sing-box-tiny run -c /etc/sing-box/config.json &
    echo $! > /var/run/sing-box-tiny.pid
}

stop() {
    killall sing-box-tiny 2>/dev/null
    rm -f /var/run/sing-box-tiny.pid
}
